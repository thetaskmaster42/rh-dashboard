# output/

`./rh-dashboard build` writes `dashboard.html` here by default.

`*.html` in this folder is gitignored (see `../.gitignore`) — a generated
dashboard contains real transaction amounts pulled from `input/`. Only this
placeholder file is tracked.
