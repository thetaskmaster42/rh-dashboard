apiVersion: v1
kind: Service
metadata:
  name: {{ include "rh-dashboard.fullname" . }}
  labels:
    {{- include "rh-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rh-dashboard.selectorLabels" . | nindent 4 }}
